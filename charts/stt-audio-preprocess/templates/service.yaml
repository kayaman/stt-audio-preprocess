apiVersion: v1
kind: Service
metadata:
  name: {{ include "stt-audio-preprocess.fullname" . }}
  labels:
    {{- include "stt-audio-preprocess.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stt-audio-preprocess.selectorLabels" . | nindent 4 }}
