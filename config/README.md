# Configuration Files

This directory can contain:

- `logging.yaml` - Custom logging configuration
- `silero_vad.onnx` - Pre-downloaded VAD model (optional)
- Custom SSL certificates if needed

By default, configurations are loaded from environment variables.
See the root `.env.example` for all available options.
